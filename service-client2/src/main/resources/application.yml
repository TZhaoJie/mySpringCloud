server:
  port: 9002

spring:
  application:
    name:
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:7001/

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10001/eureka/
