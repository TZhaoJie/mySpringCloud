server:
  port: 7002
spring:
  application:
    name: cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/TZhaoJie/mySpringCloud.git #因为github有时候不稳定，我这里改到了码云仓
          searchPaths: /properties/           #配置文件目录
          username:
          password:
          force-pull: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10001/eureka/,http://localhost:10002/eureka/

