server:
  port: 7002
spring:
  application:
    name: cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: http://git.home.com/tanzhaojie/test1.git
          searchPaths: /properties/           #配置文件目录
          username: tanzhaojie
          password: tzj258258
          force-pull: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10001/eureka/,http://localhost:10002/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS

logging:
  file: C:/Log/${spring.application.name}.log

