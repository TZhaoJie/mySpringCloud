spring:
  application:
    name: admin-server
  security:
    user:
      name: "admin"
      password: "admin"
  mail:
    #端口
    host: smtp.qq.com
    username: 1174578722@qq.com
    #授权码 不知道怎么搞可以百度
    password: lhmenlxvqvyjbage
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
  boot:
    admin:
      notify:
        mail:
          #从那里发
          from: 1174578722@qq.com
          #发到哪里
          to: 1174578722@qq.com
          #配置是否启用邮件通知 false是不起用
          enabled: true

#    mail:
#      host: smtp.163.com
#      password:
#      username: miles02

server:
  port: 6001
eureka:
  client:
    registryFetchIntervalSeconds: 5
    service-url:
      defaultZone: http://localhost:10001/eureka/,http://localhost:10002/eureka/
  instance:
    leaseRenewalIntervalInSeconds: 10
    health-check-url-path: /actuator/health
    metadata-map:
      user.name: ${spring.security.user.name}
      user.password: ${spring.security.user.password}

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS



